
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<%- include('partials/header') %>
<div class="max-w-2xl mx-auto mt-10">
  <h2 class="text-xl font-bold mb-4">All Contacts</h2>
  <!-- Loop over contacts array -->
  <% if (contacts && contacts.length > 0) { %>
    <ul class="space-y-2">
      <% contacts.forEach(contact => { %>
        <li class="border p-3 rounded">
          <strong><%= contact.name %></strong> - <%= contact.phone %>
          <div class="mt-2">
            <a href="/update-contact?id=<%= contact.id %>" class="text-blue-500 mr-4">Edit</a>
            <a href="/delete-contact?id=<%= contact.id %>" class="text-red-500">Delete</a>
          </div>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No contacts available.</p>
  <% } %>
</div>

<%- include('partials/footer') %>
