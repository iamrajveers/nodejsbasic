<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tailwind Form Example</title>
  <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CSS CDN -->
</head>

<body class="bg-gray-100 flex flex-col min-h-screen">

  <!-- Include Header -->
  <%- include('partials/header') %>

  <div class="flex-grow flex items-center justify-center">
    <div class="bg-white p-8 rounded shadow-md w-full max-w-md">

      <h1 class="text-2xl font-bold mb-6 text-center">EJS Template</h1>

      <!-- Show message if available -->
      <% if (message) { %>
        <div class="bg-green-100 text-green-700 p-4 rounded mb-4 text-center">
          <%= message %>
        </div>
      <% } %>

      <form action="/submit" method="POST" class="space-y-4">

        <div>
          <label class="block text-gray-700 font-semibold mb-2" for="myname">Enter Name:</label>
          <input 
            type="text" 
            id="myname" 
            name="myname" 
            class="w-full border border-gray-300 p-2 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
            placeholder="Enter your name"
          >
        </div>

        <div class="text-center">
          <input 
            type="submit" 
            value="Submit" 
            class="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600 cursor-pointer"
          >
        </div>

      </form>

    </div>
  </div>

  <!-- Include Footer -->
  <%- include('partials/footer') %>

</body>
</html>
